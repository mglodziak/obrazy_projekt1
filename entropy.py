import numpy as np
import matplotlib.pyplot as plt

def normalize_img(img):

def entropy(img):
    mask=input("Entry size of square mask (for example 6): ")
    width=img.shape[0]
    height=img.shape[1]
    for i in range(height):
        for j in range(width):
            
